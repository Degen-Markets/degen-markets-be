query ($betAcceptedTopic: [Bytes32!]!) {
  block {
    hash
    logs(filter: { addresses: [], topics: [$betAcceptedTopic] }) {
      data
      topics
      transaction {
        hash
        from {
          address
        }
        value
        status
      }
    }
  }
}
